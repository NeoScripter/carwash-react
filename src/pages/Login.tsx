import { Link } from "react-router"

export default function Login() {
    return (
        <div className="text-white">
            <Link to="/">Back home, the page is not ready yet</Link>
            <h1>This is the Login Page</h1>
        </div>
    );
}
